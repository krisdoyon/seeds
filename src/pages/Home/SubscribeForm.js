import React from "react";
import styled from "styled-components";

const Wrapper = styled.form``;

const SubscribeForm = () => {
  return <Wrapper>SubscribeForm</Wrapper>;
};

export default SubscribeForm;
